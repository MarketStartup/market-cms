import type { Block } from 'payload'

export const CourseListing: Block = {
   slug: 'courseListing',
   labels: {
      singular: 'Course Listing',
      plural: 'Course Listings',
   },
   fields: [

   ],
}

export const CourseDetail: Block = {
   slug: 'courseDetail',
   labels: {
      singular: 'Course Detail',
      plural: 'Course Details',
   },
   fields: [

   ],
}